# S3.2 Verification Evidence — Release Tag Guard

Date of verification: 2026-09-12

Story: S3.2 (Release Tag Guard Verification) in
`docs/sprints/sprint_3_stories.md`; steps in
`docs/analysis/S3.2-story-steps.md`. The structural suites were
executed against the working tree after commit `4a6abb4`.

## Green Pipeline Run on Main

Recorded passing pipeline run on the current `main` branch:

- Run ID: `34712597210`
- Run URL: https://github.com/daveonche/ai_assistant/actions/runs/34712597210
- Branch: `main`
- Commit: `884c675ce54512bfbad8af72ee6625642b3a64fb`
- Run date: 2026-09-12
- Outcome: success (green)

## What Was Verified

That the CI `release-tag-guard` job prevents a tag from shipping with
a stale installer pin, per the story acceptance criteria:

- `tests/test_s3_2_step1.py` confirms the `release-tag-guard` job in
  `.github/workflows/ci.yml` is triggered by `v*` tag pushes and that
  its run steps compare the pushed tag against `DEFAULT_REF` from
  `scripts/install.sh`, failing the pipeline with `exit 1` on mismatch
- `tests/test_s3_2_step2.py` confirms every `uses:` reference in
  `.github/workflows/ci.yml` resolves to a full-length commit SHA
  recorded as verified in `docs/tech_stack.md`

## Commands Run and Observed Outcomes

```bash
python -m pytest tests/test_s3_2_step1.py tests/test_s3_2_step2.py -q -s
```

Observed: `5 passed`, with the action-pin summary
`3 of 3 uses: references pinned to full-length commit SHAs`
printed for both Step 2 checks.

## Summary

The release tag guard verification criteria were all confirmed:

- The guard job triggers on `v*` tag pushes and enforces
  tag-equals-`DEFAULT_REF` with an explicit failure path (`exit 1`)
- All 3 `uses:` references in `.github/workflows/ci.yml` are pinned to
  full-length commit SHAs cross-checked against `docs/tech_stack.md`
- A green CI run on the current `main` branch (run `34712597210`,
  commit `884c675ce54512bfbad8af72ee6625642b3a64fb`, 2026-09-12) is
  recorded above as traceable evidence

No remediation change was required; the recorded run reflects the
pipeline definition state confirmed by the structural checks.

## How to Re-verify

Run from the project root:

```bash
python -m pytest tests/test_s3_2_step1.py tests/test_s3_2_step2.py -q -s
git --no-pager status --porcelain
```

Expected: `5 passed` and empty status output.
