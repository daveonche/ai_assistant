Story S2.2 Implementation Steps:

Step 1. Enable the exact host runtime version pin in the technology-stack documentation
Must Support:
- A single exact host runtime version is selected as the supported pin
- The exact version and its rationale are recorded in docs/tech_stack.md,
  following the existing version-lock rationale format in that file
- The recorded pin resolves the version item flagged during story S1.2
Manual Verification:
- Open docs/tech_stack.md and confirm an exact version (not a range or
  minimum) is recorded with a rationale in the established format
- Confirm the recorded version matches the runtime actually used on the
  host when developing and testing this project
Prerequisite: None
Developer Notes:
- Documentation-level change only; no launcher or script behavior changes
- Reuse the existing version-lock rationale entries as the format model
- The chosen version should reflect what the team genuinely develops and
  tests against, not an aspirational target

Step 2. Enable the resolved minimum runtime version for the assistant package
Must Support:
- The minimum supported runtime version for the assistant package is
  confirmed and justified
- The minimum is stated in the .agent/pyproject.toml package metadata
- The justification is recorded in docs/tech_stack.md in the established
  rationale format, consistent with the Step 1 host pin
- The resolution clears the version item flagged during story S1.4
Manual Verification:
- Open .agent/pyproject.toml and confirm the declared minimum runtime
  version is present
- Compare the declared minimum against the Step 1 host pin in
  docs/tech_stack.md and confirm they are mutually consistent
Prerequisite: Step 1 (the exact host runtime pin is the basis for the
package minimum)
Developer Notes:
- Config-level change only; keep the declaration minimal and justified
- The minimum must never exceed the pinned host runtime version
- Record why this minimum and not a lower one, in the established format

Step 3. Enable consistent prerequisite documentation across the readme and compatibility matrix
Must Support:
- README prerequisites state the same supported runtime version
  information as docs/tech_stack.md
- The compatibility matrix in docs/tech_stack.md reflects the pinned
  host runtime version and the resolved package minimum
- README, docs/tech_stack.md, and .agent/pyproject.toml agree with no
  contradictory version statements
Manual Verification:
- Read the README prerequisites and the docs/tech_stack.md
  compatibility matrix side by side; confirm identical version numbers
- Cross-check both against .agent/pyproject.toml; confirm all three
  sources agree
Prerequisite: Steps 1 and 2 (the pinned values must exist before the
documents can be aligned)
Developer Notes:
- Alignment pass only; avoid rewording unrelated documentation content
- Resolve contradictions by updating toward the recorded pins, not by
  introducing new version candidates

Step 4. Enable resolved-status annotations for the flagged version items
Must Support:
- The version items flagged in docs/sprints/sprint_1_stories.md (the
  S1.2 and S1.4 items) are updated to resolved status
- Each annotation makes the resolution visible to a reviewer, pointing
  at the recorded pin or justification
- No flagged version-pin items remain in the sprint story documentation
Manual Verification:
- Open docs/sprints/sprint_1_stories.md and confirm both previously
  flagged items now show resolved status
- Search the sprint story documentation for any remaining flagged
  version-pin items; confirm none remain
Prerequisite: Steps 1 and 2 (the resolutions must exist before the
annotations can reference them)
Developer Notes:
- Annotation-only change; do not alter the historical story text beyond
  the flag status
- Keep the resolved annotations in the same style as the existing flags
