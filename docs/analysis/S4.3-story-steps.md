Story S4.3 Implementation Steps:

Step 1. Enable a corrected automatic-increment example in the release documentation
Must Support:
- The documented automatic-increment example shows the version's patch segment changing from one release to the next
- The example's before-and-after versions stay consistent with the example pair shown in the release script's usage text
Manual Verification:
- Open the release documentation's "cutting a release" guidance and locate the automatic-increment example
- Confirm the example shows the patch segment changing (for example, one patch version to the next) and does not contradict the release script's usage example
Prerequisite: None
Developer Notes:
- Keep the example values aligned with the usage text so a reader can predict the resulting version
- Scope the edit to the example; leave surrounding guidance unchanged

Step 2. Enable confirmation that the documentation edit preserves release-reference consistency
Must Support:
- Every pinned release reference string checked by the project's verification suites remains unchanged by the edit
- The project's verification suites complete with no failures after the edit
Manual Verification:
- Run the project's full verification suite after making the edit
- Observe every check pass, including the release-reference consistency checks
Prerequisite: Step 1
Developer Notes:
- Avoid touching any pinned version strings elsewhere in the documentation
- If a check fails, restore the unintended change and re-run the suite

Step 3. Enable documentation-conventions conformance for the edited section
Must Support:
- The edited section conforms to the project's documentation conventions
Manual Verification:
- Review the edited section against the project's documentation conventions reference
- Observe no convention violations in structure, fencing, or line style
Prerequisite: Step 1
Developer Notes:
- Keep the change minimal so the review stays focused on the corrected example
