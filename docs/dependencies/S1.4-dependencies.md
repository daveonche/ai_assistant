# Dependency Report S1.4

## Core Framework/Runtime Dependencies

- pytest==8.3.5 — test runner for the unit-test suites (declared in
  `.agent/pyproject.toml`; installed in the aider container via
  `.agent/Dockerfile.aider` Layer D3)

## Story-Specific Dependencies

- PyYAML==6.0.3 — YAML parsing (`yaml.safe_load()`) for validating
  `.agent/.aider.conf.yml` in the S1.4 Step 1 tests. Newest stable release
  (`pip index` LATEST), already present in the test container (bundled
  transitively by aider-chat), no peer requirements, compatible with the
  fixed pytest==8.3.5 set.

## Other Relevant Dependencies

- setuptools>=61 — build-system requirement only (`[build-system]` in
  `.agent/pyproject.toml`), not a project dependency
- Python >=3.8 — declared runtime minimum (`requires-python`); PyYAML
  6.0.3 verified on the container's Python 3.12

All versions are locked and verified compatible.
