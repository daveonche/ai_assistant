# Dependency Report S3.2

## Core Framework/Runtime Dependencies
- Python >=3.12 (host pin 3.12.12; standard library only)
- pytest==8.3.5
- PyYAML==6.0.3 (workflow YAML parsing in the S1.6 verification tests)

## Story-Specific Dependencies
None — S3.2 is verification-and-evidence only: it inspects the existing
`release-tag-guard` job in `.github/workflows/ci.yml` and records a
green CI run. No new packages are introduced.

## Other Relevant Dependencies
- GitHub Actions runner tooling (environment-provided, not a package
  dependency): git and the existing SHA-pinned `actions/checkout`
- Existing fixed stack unchanged: `.agent/pyproject.toml`
  (pytest==8.3.5, PyYAML==6.0.3)

All versions are locked and verified compatible.
