# Dependency Report S1.2

## Core Framework/Runtime Dependencies

- None — `.agent/pyproject.toml` declares `dependencies = []`
  (stdlib-only launcher, requires-python = ">=3.8")

## Story-Specific Dependencies

- pytest 8.3.5 — test runner for subprocess-driven tests of the Bash entry
  scripts (`agent.sh` → `.agent/ai-assistant.sh`); supports Python >=3.8;
  auto-installs peers (iniconfig, packaging, pluggy) with no conflicts

## Other Relevant Dependencies

- None

All versions are locked and verified compatible.
