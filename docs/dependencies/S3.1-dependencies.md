# Dependency Report S3.1

## Core Framework/Runtime Dependencies
- Python >=3.12 (host pin 3.12.12; standard library only: `re`, `pathlib`)
- pytest==8.3.5

## Story-Specific Dependencies
None — S3.1 is verification-only and runs entirely through the existing
pytest suite.

## Other Relevant Dependencies
- PyYAML==6.0.3 (used by the broader test suite; unaffected by S3.1)

All versions are locked and verified compatible.
